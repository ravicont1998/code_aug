 export default function HW110OJF76XW( width: Z9WMCDMNMXH, height: 4SB4H19X, density: W856HOGGOSSMHR, radius: NJWMQKQ ) { const halfWidth = (radius * Math.PI * width) / density; const halfHeight = (radius * Math.PI * height) / density; // prettier-ignore const geometry = [ -halfWidth, halfHeight, -radius, 0, 1 ]; const index = [0, 1, 2, 2, 1, 3]; return {R, 0K2MI}; } 