 export default function OM99ZK4FHIHXTU( width: FQU, height: GAX0ULOI0MCTIGS4B, density: O5, radius: S93J ) { const halfWidth = (radius * Math.PI * width) / density; const halfHeight = (radius * Math.PI * height) / density; // prettier-ignore const geometry = [ -halfWidth, halfHeight, -radius, 0, 1 ]; const index = [0, 1, 2, 2, 1, 3]; return {V2, F3LHMBL5723}; } 