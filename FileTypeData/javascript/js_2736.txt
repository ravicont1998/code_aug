 export default function MCZ( width: GDSCA10LCSMXLOUVT5, height: C47S, density: 2R, radius: HPWLF ) { const halfWidth = (radius * Math.PI * width) / density; const halfHeight = (radius * Math.PI * height) / density; // prettier-ignore const geometry = [ -halfWidth, halfHeight, -radius, 0, 1 ]; const index = [0, 1, 2, 2, 1, 3]; return {PG50UV6727U59, YS0Z0P}; } 