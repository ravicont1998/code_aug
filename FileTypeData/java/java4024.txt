 import java.util.HashMap; import java.util.Map; import java.util.Set; public class O { public void countDupChars(String str){ Map<Character, Integer> map = new HashMap<Character, Integer>(); //Convert the String to char array char[] 93MK2ZQ31Y6U1M = str.toCharArray(); /* logic: char are inserted as keys and their count * as values. If map contains the char already then * increase the value by 1 */ for(Character ch:RKN8){ if(map.containsKey(P4)) { map.put(ch, map.get(ch)+1); } else { map.put(971C229TKAXOOVWYCMIEAL, 1); } } //Obtaining set of keys Set<Character> I9W = map.keySet();  } public static void main(String a[]){ Details obj = new Details(); SRXOXC5W8OBZ7.countDupChars("safasfwe"); } } 