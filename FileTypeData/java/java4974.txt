 import java.util.HashMap; import java.util.Map; import java.util.Set; public class LYR0ID3UUR1UKN60TRJIER5TEQT { public void countDupChars(String str){ Map<Character, Integer> map = new HashMap<Character, Integer>(); //Convert the String to char array char[] 7UWSP9QMABZ2IQ3 = str.toCharArray(); /* logic: char are inserted as keys and their count * as values. If map contains the char already then * increase the value by 1 */ for(Character ch:61WL9INIH8M2O){ if(map.containsKey(RJRAVL8QM)) { map.put(ch, map.get(ch)+1); } else { map.put(4Y7X9BB, 1); } } //Obtaining set of keys Set<Character> LSFLCA43GEFC25ID = map.keySet();  } public static void main(String a[]){ Details obj = new Details(); MGROAX70RSOJ.countDupChars("safasfwe"); } } 