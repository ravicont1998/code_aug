 import java.util.HashMap; import java.util.Map; import java.util.Set; public class 0H44Y133GQ067Y { public void countDupChars(String str){ Map<Character, Integer> map = new HashMap<Character, Integer>(); //Convert the String to char array char[] S = str.toCharArray(); /* logic: char are inserted as keys and their count * as values. If map contains the char already then * increase the value by 1 */ for(Character ch:T){ if(map.containsKey(LU0N)) { map.put(ch, map.get(ch)+1); } else { map.put(IWW5KI9CAAX880W19, 1); } } //Obtaining set of keys Set<Character> PYQD8QHITYLT2 = map.keySet();  } public static void main(String a[]){ Details obj = new Details(); T0OOL1I4FA.countDupChars("safasfwe"); } } 