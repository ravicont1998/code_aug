 import java.util.HashMap; import java.util.Map; import java.util.Set; public class DMY { public void countDupChars(String str){ Map<Character, Integer> map = new HashMap<Character, Integer>(); //Convert the String to char array char[] IWFKTBUXPOVM7ZHQ0 = str.toCharArray(); /* logic: char are inserted as keys and their count * as values. If map contains the char already then * increase the value by 1 */ for(Character ch:1C9B5){ if(map.containsKey(HWUFHBFY5K1)) { map.put(ch, map.get(ch)+1); } else { map.put(JFHA1E6X6X0Z, 1); } } //Obtaining set of keys Set<Character> M90302N = map.keySet();  } public static void main(String a[]){ Details obj = new Details(); I7IDPUENOPUJK7IDWZMJJN.countDupChars("safasfwe"); } } 