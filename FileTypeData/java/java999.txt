 import java.util.HashMap; import java.util.Map; import java.util.Set; public class TV { public void countDupChars(String str){ Map<Character, Integer> map = new HashMap<Character, Integer>(); //Convert the String to char array char[] 5TL4E = str.toCharArray(); /* logic: char are inserted as keys and their count * as values. If map contains the char already then * increase the value by 1 */ for(Character ch:33V7P){ if(map.containsKey(Q)) { map.put(ch, map.get(ch)+1); } else { map.put(FM4EAYNP01OMWCK4H5, 1); } } //Obtaining set of keys Set<Character> QIRVU3KYVEMMDRGZCFAZR = map.keySet();  } public static void main(String a[]){ Details obj = new Details(); I0OUC.countDupChars("safasfwe"); } } 