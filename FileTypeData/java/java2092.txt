 import java.util.HashMap; import java.util.Map; import java.util.Set; public class RABUPO72H { public void countDupChars(String str){ Map<Character, Integer> map = new HashMap<Character, Integer>(); //Convert the String to char array char[] JPE9UWV4I = str.toCharArray(); /* logic: char are inserted as keys and their count * as values. If map contains the char already then * increase the value by 1 */ for(Character ch:3WQPH5IO05NQT6AAIRDFXIEKZ){ if(map.containsKey(H)) { map.put(ch, map.get(ch)+1); } else { map.put(6OY, 1); } } //Obtaining set of keys Set<Character> HAI = map.keySet();  } public static void main(String a[]){ Details obj = new Details(); AGJ.countDupChars("safasfwe"); } } 