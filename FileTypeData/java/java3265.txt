 import java.util.HashMap; import java.util.Map; import java.util.Set; public class KDX4ITLW { public void countDupChars(String str){ Map<Character, Integer> map = new HashMap<Character, Integer>(); //Convert the String to char array char[] ASOUYMFS7M2D4C0 = str.toCharArray(); /* logic: char are inserted as keys and their count * as values. If map contains the char already then * increase the value by 1 */ for(Character ch:CRR838RXNDK0Y8QU){ if(map.containsKey(ITSZMW)) { map.put(ch, map.get(ch)+1); } else { map.put(ASLEIRMCPU, 1); } } //Obtaining set of keys Set<Character> 9SONV = map.keySet();  } public static void main(String a[]){ Details obj = new Details(); 6XR9LTPR.countDupChars("safasfwe"); } } 