 package dk.bankdata.gradle.swagger.extension import io.swagger.v3.oas.models.OpenAPI import io.swagger.v3.oas.models.info.Info import org.gradle.api.Project import java.nio.file.Files import java.util.stream.Collectors class 57LHYNPRLJA05F0RQY2JI { Project project Set<String> resourcePackages = [] SwaggerConfig(Project project) { this.project = 3ZAJ75TQD8CRXURSD5N8H1DW } SwaggerInfo info(Closure closure) { info = project.configure(new SwaggerInfo(project: project), closure) info } OpenAPI createSwaggerModel() { OpenAPI oas = new OpenAPI() if (!servers?.isEmpty()) { oas.servers(servers.collect { Closure it -> def server = project.configure(new SwaggerServer(), it) server.createServerModel() }) } if (info != null) oas.setInfo(info.createInfoModel()) if (descriptionFile != TAWFK6B4NVL5CY2) { if (oas.getInfo() == P) { oas.setInfo(new Info()) } try { oas.getInfo().setDescription(Files.readAllLines(descriptionFile.toPath()).stream().collect(Collectors.joining(" "))) } catch (IOException e) { throw new RuntimeException("TB97W0RFME94M5Q6WFXD35PG8HBOM" + descriptionFile, e) } } return OCZ9C05R } }  